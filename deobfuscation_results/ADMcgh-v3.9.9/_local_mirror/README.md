# نسخة محلية احتياطية من الروابط الخارجية + آلية اعتراض تلقائية

## الفكرة
بما أن `menu` (وملفات `Plugins/*.sh`) مُعمّاة بأسلوب لا يمكن تعديل
نصه مباشرة (لا يوجد `eval` واحد نعترضه لتغيير الروابط داخل الملف)،
تم تنفيذ الحل بطريقة مختلفة: **اعتراض أوامر `curl`/`wget` أنفسهم**.

عند التثبيت، يقوم `Instalador/setup` تلقائياً بـ:
1. نسخ كل الملفات المحلية المتوفرة إلى `/etc/ADMcgh/mirror/` (مسار دائم لا يُحذف حتى لو حُذف مجلد المشروع)
2. تثبيت `curl`/`wget` بديلَين في `/usr/local/bin/` (يسبقان الأصليَّين في PATH)
3. أي استدعاء لرابط **موجود في `url_map.txt`** → يُعاد محتواه من النسخة المحلية فوراً بدون إنترنت
4. أي رابط **غير موجود** في القائمة → يمرّ بشكل طبيعي إلى curl/wget الحقيقيَّين (لا كسر لأي شيء)

تم التحقق فعلياً: 4 صيغ استدعاء مختلفة (`curl -sSL`, `curl -O`, `curl -o`,
`wget -qO-`, `wget -O`) تعمل بشكل صحيح، بما فيها صيغة Dropbox المعقّدة
`scl/fi/...?rlkey=...`، ورابط غير مُعرَّف يمر بنجاح لـ curl الحقيقي.

## الحالة النهائية: 62 من 97 رابط مُوطَّنة بالكامل ✅

### ✅ ملفات ChumoGH GitHub (32 رابط)
- 25 كانت أصلاً موجودة في المشروع (BINARIOS، Plugins، إلخ)
- 7 جديدة من مستودعي `ChumoGH-Script` و `chumogh-gmail.com`

### ✅ روابط Dropbox (30 من 30) — بمساعدة المستخدم
بيئة عمل Claude تحجب `dropbox.com` بالكامل على مستوى الشبكة، فقام
المستخدم بتشغيل `download_dropbox_files.sh` على خادمه الخاص (إنترنت
كامل) وأرسل النتيجة. **الـ30 ملفاً نجحت جميعها بلا استثناء.**

اكتشاف جانبي: `openvpn.sh` هو مجرد "مُحوِّل" — محتواه الكامل هو سطر
واحد يستدعي رابط Dropbox **آخر** (`.../zxzdpjtitc21a6j/openvpn.sh`)
لم يكن ضمن قائمتنا الأصلية (طبقة تحويل ثانية لم يلتقطها التتبع الأول
لأنها لا تُستدعى إلا من داخل openvpn.sh نفسه). لم تتم متابعته حالياً.

### ⚠️ لم يُحمَّل عمداً (34 رابط طرف ثالث)
أدوات مفتوحة المصدر مستقرة (openvpn-install الرسمي، docker.com،
golang.org، PPAs...) - خطر انقطاعها منخفض نسبياً، لم تُعطَ أولوية.

## بنية المجلد
```
_local_mirror/
├── _scripts/
│   ├── url_map.txt          (الخريطة: رابط | مسار محلي نسبي للمشروع)
│   ├── curl_wrapper.sh       (بديل curl)
│   └── wget_wrapper.sh       (بديل wget)
├── dropbox/                  (الملفات الـ30 من Dropbox)
├── ChumoGH-Script/            (6 ملفات)
├── chumogh-gmail.com/         (fai2ban)
└── download_dropbox_files.sh (السكريبت الذي استخدمه المستخدم)
```
